# Loaded by `next dev` only.
#
# In production the Worker serves /signaling on the app's own origin, so the
# client derives the URL from window.location. `next dev` has no signaling
# handler, so dev points at the standalone signaling-server.mjs instead.
NEXT_PUBLIC_YWEBRTC_SIGNALING_URL=ws://localhost:3001
