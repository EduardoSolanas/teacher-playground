min-release-age=7
; npm 12 refuses tarball-URL dependencies by default. The Excalidraw fork is
; one (a GitHub release package.tgz); `root` allows URLs only when this
; package.json names them, never when a dependency pulls one in.
allow-remote=root
